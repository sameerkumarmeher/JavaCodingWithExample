package com.java.AccessSpecifier;

/*
Constructor level Access Specifier
->If you make a Constructor private then its object can be created in same class but not outside the class
->If we make a constructor is default/protected then its object can be created in same class and same package
   but not outside the package.
 */
public class C {
}
